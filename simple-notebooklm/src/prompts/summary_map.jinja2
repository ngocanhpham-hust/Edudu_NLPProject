Tóm tắt nhóm nguồn sau thành JSON hợp lệ để dùng trong bước map-reduce.
Schema:
{
  "summary": "tóm tắt nhóm nguồn",
  "key_points": ["ý chính quan trọng"]
}

Nguồn:
{% for chunk in chunks %}
[S{{ loop.index }}] {{ chunk.metadata.filename }} - trang {{ chunk.metadata.page }}
{{ chunk.text }}
{% endfor %}
