[project]
name = "final-notebooklm-study-assistant"
version = "1.0.0"
description = "Lightweight NotebookLM-inspired NLP Study Assistant for PDF documents"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
